{
  "id": "kafka-readonly",
  "display_name": "Kafka Read-only MCP",
  "priority": "P2",
  "status": "optional",
  "capabilities": [
    "event-topology-read-only"
  ],
  "default_enabled": false,
  "risk": "high",
  "roles": {
    "allowed": [
      "system-analyst",
      "developer-backend",
      "devops-engineer",
      "technical-leader"
    ],
    "optional": [
      "qa-engineer"
    ],
    "forbidden": [
      "business-analyst",
      "developer-frontend",
      "project-manager",
      "general-coordinator"
    ]
  },
  "runtime": {
    "transport": "stdio",
    "implementation": "external-or-adapter-specific",
    "source_url": "TBD approved Kafka MCP implementation",
    "install_location": "/opt/eksad-mcp/bin/kafka-readonly"
  },
  "env_contract": [
    {
      "name": "KAFKA_READONLY_BOOTSTRAP",
      "required": true,
      "secret": true,
      "description": "Read-only bootstrap/credential contract."
    }
  ],
  "hermes_config": {
    "server_name": "kafka_readonly",
    "command": "/opt/eksad-mcp/bin/kafka-readonly",
    "args": [],
    "env": {
      "KAFKA_READONLY_BOOTSTRAP": "${KAFKA_READONLY_BOOTSTRAP}"
    },
    "timeout": 120,
    "connect_timeout": 60,
    "sampling": {
      "enabled": false
    }
  },
  "generic_harness": {
    "capability_contract": [
      "event-topology-read-only"
    ],
    "requires_explicit_runtime_binding": true,
    "fallback_required": true
  },
  "notes": "Topology/metadata only; no publish/consumer-group mutation by default."
}
