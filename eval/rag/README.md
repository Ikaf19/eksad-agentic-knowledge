# RAG Evaluation Fixtures

These fixtures define what a future RAG runtime should prove before it is exposed to Hermes or another agentic harness.

They are schema/evidence fixtures only. They do not call an LLM, embedding model, vector DB, or LiteLLM gateway.

Run:

```bash
python3 eval/rag/scripts/validate-rag-eval.py
```
