# Optional Hermes MCP example — kafka-readonly
# Not default-enabled. Add only when real project workflow requires it.

mcp_servers:
  kafka_readonly:
    command: "/opt/eksad-mcp/bin/kafka-readonly"
    args: []
    env: {}
    timeout: 120
    connect_timeout: 60
    sampling:
      enabled: false
