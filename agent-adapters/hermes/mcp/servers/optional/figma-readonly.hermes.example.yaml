# Optional Hermes MCP example — figma-readonly
# Not default-enabled. Add only when real project workflow requires it.

mcp_servers:
  figma_readonly:
    command: "/opt/eksad-mcp/bin/figma-readonly"
    args: []
    env: {}
    timeout: 120
    connect_timeout: 60
    sampling:
      enabled: false
