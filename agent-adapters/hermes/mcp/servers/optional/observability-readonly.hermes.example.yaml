# Optional Hermes MCP example — observability-readonly
# Not default-enabled. Add only when real project workflow requires it.

mcp_servers:
  observability_readonly:
    command: "/opt/eksad-mcp/bin/observability-readonly"
    args: []
    env: {}
    timeout: 120
    connect_timeout: 60
    sampling:
      enabled: false
