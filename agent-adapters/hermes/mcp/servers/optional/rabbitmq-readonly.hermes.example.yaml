# Optional Hermes MCP example — rabbitmq-readonly
# Not default-enabled. Add only when real project workflow requires it.

mcp_servers:
  rabbitmq_readonly:
    command: "/opt/eksad-mcp/bin/rabbitmq-readonly"
    args: []
    env: {}
    timeout: 120
    connect_timeout: 60
    sampling:
      enabled: false
