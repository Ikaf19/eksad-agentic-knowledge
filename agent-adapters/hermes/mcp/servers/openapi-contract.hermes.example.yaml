# Hermes MCP example — OpenAPI contract
# Placeholder only. Replace command/url with an approved MCP server implementation.
# Secrets must come from local env, not Git.

mcp_servers:
  openapi_contract:
    command: "/opt/eksad-mcp/bin/openapi-contract"
    args: []
    env:
      OPENAPI_CONTRACT_TOKEN: "${OPENAPI_CONTRACT_TOKEN}"
    timeout: 120
    connect_timeout: 60
    sampling:
      enabled: false
