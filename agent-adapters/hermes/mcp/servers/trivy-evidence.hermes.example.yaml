# Hermes MCP example — Trivy evidence
# Placeholder only. Replace command/url with an approved MCP server implementation.
# Secrets must come from local env, not Git.

mcp_servers:
  trivy_evidence:
    command: "/opt/eksad-mcp/bin/trivy-evidence"
    args: []
    env:
      TRIVY_EVIDENCE_TOKEN: "${TRIVY_EVIDENCE_TOKEN}"
    timeout: 120
    connect_timeout: 60
    sampling:
      enabled: false
